description: Request forbidden
content:
  application/json:
    schema:
      type: object
      properties:
        status:
          $ref: ../../types/http/status.yaml
        message:
          type: string
        data:
          type: object
      example:
        {
          "status": 403,
          "message": "You do not have permissions to access this functionality",
          "data": {},
        }
