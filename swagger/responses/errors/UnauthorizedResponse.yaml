description: Authentication required
content:
  application/json:
    schema:
      type: object
      properties:
        status:
          $ref: ../../types/http/status.yaml
        error:
          type: string
        message:
          type: string
        params:
          type: object
          additionalProperties:
            type: string
      example:
        {
          "code": "AA-0001",
          "message": "You should provide a valid access token",
          "params": {},
        }
