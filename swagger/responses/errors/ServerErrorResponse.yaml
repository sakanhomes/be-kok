description: Server error response
content:
  application/json:
    schema:
      type: object
      properties:
        status:
          $ref: ../../types/http/status.yaml
        message:
          type: string
        data:
          type: object
      example:
        {
          "status": 500,
          "message": "An error occured on server side, please try again later",
          "data": {},
        }
