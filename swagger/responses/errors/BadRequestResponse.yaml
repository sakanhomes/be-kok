description: Bad request response
content:
  application/json:
    schema:
      type: object
      properties:
        status:
          $ref: ../../types/http/status.yaml
        message:
          type: string
        data:
          type: object
      example:
        {
          "status": 400,
          "message": "Cannot process data you have sent",
          "data": {},
        }
