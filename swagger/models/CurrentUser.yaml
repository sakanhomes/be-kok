title: CurrentUser
type: object
properties:
  address:
    $ref: ../types/crypto/address.yaml
  name:
    type: string
    nullable: true
    example: John Smith
  profileImage:
    allOf:
      - $ref: ../types/file_url.yaml
      - nullable: true
  backgroundImage:
    allOf:
      - $ref: ../types/file_url.yaml
      - nullable: true
  description:
    type: string
    nullable: true
    example: Long profile description
  balance:
    type: number
    min: 0
    example: 123.45
